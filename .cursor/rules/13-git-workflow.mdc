---
description: Git-ի աշխատանքի կանոններ։ Ճյուղեր, commit-ներ, PR, merge ռազմավարություն։
globs: ["**/*"]
alwaysApply: false
---

# GIT WORKFLOW-Ի ԿԱՆՈՆՆԵՐ

> Մաքուր պատմություն = հասկանալի պատմություն։ Համահունչ commit-ներ = պարզ revue։

---

## 🎯 ՀԻՄՆԱԿԱՆ ՍԿԶԲՈՒՆՔՆԵՐ

1. **Conventional Commits** — ստանդարտացված հաղորդագրություններ
2. **Feature Branches** — յուրաքանչյուր խնդիր իր ճյուղում
3. **Small PRs** — փոքր, կենտրոնացված փոփոխություններ
4. **Linear History** — մաքուր գծային պատմություն (որտեղ հնարավոր է)

---

## 🌿 ՃՅՈՒՂԵՐԻ ՌԱԶՄԱՎԱՐՈՒԹՅՈՒՆ

### Հիմնական ճյուղեր

```
main (production)
├── develop (staging) — միջին/մեծ նախագծերի համար
│   ├── feature/add-payment
│   ├── feature/user-profile
│   └── bugfix/cart-total
└── hotfix/critical-fix — արտադրության արտակարգ ֆիքսեր
```

### Փոքր նախագծերի համար (A)

```
main (production + development)
├── feature/xxx
├── bugfix/xxx
└── hotfix/xxx
```

### Միջին/մեծ նախագծերի համար (B, C)

```
main (production)
├── develop (staging/preview)
│   ├── feature/xxx
│   ├── bugfix/xxx
│   └── refactor/xxx
└── hotfix/xxx (ճյուղվում է main-ից)
```

---

## 📛 ՃՅՈՒՂԵՐԻ ԱՆՎԱՆԱՎՈՐՄԱՆ ԿԱՆՈՆՆԵՐ

### Ձևաչափ

```
<type>/<ticket-id?>-<short-description>
```

### Ճյուղերի տիպեր

| Տիպ | Երբ օգտագործել | Օրինակ |
|-----|-----------------|--------|
| `feature/` | Նոր ֆունկցիոնալություն | `feature/add-cart` |
| `bugfix/` | Բագի ուղղում | `bugfix/fix-login-error` |
| `hotfix/` | Արտադրության արտակարգ ֆիքս | `hotfix/critical-payment-fix` |
| `refactor/` | Ռեֆակտորինգ առանց նոր ֆունկցիայի | `refactor/auth-module` |
| `docs/` | Միայն փաստաթղթավորում | `docs/api-endpoints` |
| `test/` | Միայն թեստեր | `test/add-order-tests` |
| `chore/` | Ինֆրակառուցվածք, կախվածություններ | `chore/update-deps` |

### Անվանավորման կանոններ

```bash
# ✅ ՃԻՇՏ
feature/user-authentication
feature/JIRA-123-add-payment
bugfix/cart-total-calculation
refactor/extract-api-client
chore/upgrade-nextjs-15

# ❌ ՍԽԱԼ
feature_add_payment        # Subscript
Feature/AddPayment         # CamelCase
my-feature                 # Առանց տիպի
feature/add_payment_method # Խառը ոճ
```

---

## 💬 CONVENTIONAL COMMITS

### Հաղորդագրության ձևաչափ

```
<type>(<scope?>): <subject>

<body?>

<footer?>
```

### Commit-ների տիպեր

| Տիպ | Երբ օգտագործել | CHANGELOG |
|-----|-----------------|-----------|
| `feat` | Նոր ֆունկցիոնալություն | Minor ↑ |
| `fix` | Բագի ուղղում | Patch ↑ |
| `docs` | Միայն փաստաթղթավորում | - |
| `style` | Ֆորմատավորում (ոչ CSS) | - |
| `refactor` | Ռեֆակտորինգ | - |
| `perf` | Արտադրողականության բարելավում | Patch ↑ |
| `test` | Թեստերի ավելացում | - |
| `build` | Հավաքում, կախվածություններ | - |
| `ci` | CI/CD կոնֆիգուրացիա | - |
| `chore` | Այլ փոփոխություններ | - |
| `revert` | Փոփոխությունների հետարկում | - |

### Breaking Changes

```bash
# Breaking change-ով
feat(api)!: change response format

BREAKING CHANGE: Response now wrapped in { data: ... }

# Կամ footer-ում
feat(auth): add OAuth support

BREAKING CHANGE: removed password login
```

### Commit-ների օրինակներ

```bash
# ✅ ԼԱՎ — տեղեկատվական
feat(cart): add quantity selector to cart items
fix(auth): handle expired token refresh
docs(readme): add deployment instructions
refactor(api): extract validation to separate service
perf(images): implement lazy loading for product gallery
test(orders): add integration tests for checkout flow

# ❌ ՎԱՏ — ոչ տեղեկատվական
fix: bug
update code
WIP
changes
asdf
```

### Scope (շրջանակ)

```bash
# Ֆիչ/մոդուլով
feat(cart): ...
feat(auth): ...
feat(products): ...

# Շերտով
fix(api): ...
fix(ui): ...
fix(db): ...

# Scope առանց, եթե գլոբալ
chore: update dependencies
docs: add contributing guide
```

### Commit-ի կաղապար

```bash
# ~/.gitmessage

# <type>(<scope>): <subject>
# |<----  Using a Maximum Of 50 Characters  ---->|

# Body (optional)
# |<----   Try To Limit Each Line to a Max Of 72 Characters   ---->|

# Footer (optional)
# BREAKING CHANGE: <description>
# Closes #123
```

```bash
# Կաղապարի օգտագործում
git config commit.template ~/.gitmessage
```

---

## 🔀 PULL REQUESTS

### PR-ի կանոններ

1. **Մեկ PR = մեկ առաջադրանք** — մի խառնիր կապ չունեցող փոփոխություններ
2. **Փոքր չափ** — մինչև 400 տող (ideally մինչև 200)
3. **Self-review** — ստուգի՛ր իր PR-ը revue-ից առաջ
4. **Թեստերն անցնում են** — CI-ն պետք է կանաչ լինի
5. **Նկարագրությունը լրացված** — ինչ, ինչու, ինչպես թեստավորել

### PR-ի կաղապար

```markdown
## Նկարագրություն

Փոփոխությունների համառոտ նկարագրություն։

## Փոփոխության տիպ

- [ ] 🚀 Նոր ֆունկցիոնալություն (feature)
- [ ] 🐛 Բագի ուղղում (bugfix)
- [ ] 🔧 Ռեֆակտորինգ (refactor)
- [ ] 📚 Փաստաթղթավորում (docs)
- [ ] ⚡ Արտադրողականություն (perf)

## Կապված առաջադրանքներ

- Closes #123
- Related to #456

## Փոփոխություններ

- Ավելացվել է X կոմպոնենտը
- Ուղղվել է Y վալիդացիան
- Z մոդուլի ռեֆակտորինգ

## Screenshot-ներ (UI-ի դեպքում)

| Before | After |
|--------|-------|
| ... | ... |

## Ինչպես թեստավորել

1. Քայլ 1
2. Քայլ 2
3. Ակնկալվող արդյունք

## Checklist

- [ ] Կոդը համապատասխանում է նախագծի ստանդարտներին
- [ ] Թեստեր ավելացվել/թարմացվել են
- [ ] Փաստաթղթավորումը թարմացվել է
- [ ] Self-review կատարվել է
- [ ] CI-ն անցնում է
```

### PR-ի անվանում

```bash
# ✅ ՃԻՇՏ
feat(cart): add quantity selector
fix(auth): handle expired token
docs: update API documentation

# ❌ ՍԽԱԼ
Update
Fix bug
WIP
```

---

## 🔄 MERGE ռազմավարություններ

### Squash Merge (խորհուրդ՝ feature-ի համար)

```bash
# Ճյուղի բոլոր commit-ները միաձուլվում են մեկի մեջ
git merge --squash feature/xxx
```

**Լավ.** Մաքուր պատմություն main-ում, մեկ commit = մեկ ֆիչ
**Վատ.** Ճյուղի մանրամասն պատմությունը կորչում է

### Merge Commit (release/hotfix-ի համար)

```bash
# Ստեղծվում է merge commit
git merge --no-ff hotfix/xxx
```

**Լավ.** Երևում է, թե որտեղից եկան փոփոխությունները
**Վատ.** Բարդացնում է պատմությունը

### Rebase (feature ճյուղի թարմացման համար)

```bash
# Ճյուղը rebase վերջին main-ի վրա
git checkout feature/xxx
git rebase main
```

**Լավ.** Գծային պատմություն, merge commit-ներ չկան
**Վատ.** Փոխում է պատմությունը, պահանջում է force push

### Խորհուրդներ

```markdown
## Երբ ինչ օգտագործել

| Իրավիճակ | Ռազմավարություն |
|----------|-----------|
| Feature → develop/main | Squash Merge |
| Hotfix → main | Merge Commit |
| Develop → main (release) | Merge Commit |
| Feature ճյուղի թարմացում | Rebase |
```

---

## 📋 GIT WORKFLOW

### Առաջադրանքի վրա աշխատանքի սկիզբ

```bash
# 1. Թարմացնել main/develop
git checkout main
git pull origin main

# 2. Ստեղծել ճյուղ
git checkout -b feature/add-cart

# 3. Աշխատել, commit անել
git add .
git commit -m "feat(cart): add cart page layout"
git commit -m "feat(cart): implement add to cart"
git commit -m "test(cart): add cart tests"

# 4. PR-ից առաջ — թարմացնել main-ից
git fetch origin
git rebase origin/main

# 5. Push և PR ստեղծել
git push -u origin feature/add-cart
```

### Ուղղում revue-ից հետո

```bash
# Ավելացնել fix-եր
git add .
git commit -m "fix(cart): address review comments"

# ԿԱՄ amend վերջին commit-ը (մանր ուղղումների դեպքում)
git add .
git commit --amend --no-edit
git push --force-with-lease
```

### Main-ի հետ սինխրոնացում

```bash
# Rebase (նախընտրելի feature-ի համար)
git fetch origin
git rebase origin/main
git push --force-with-lease

# ԿԱՄ Merge (եթե rebase-ը բարդ է)
git fetch origin
git merge origin/main
git push
```

---

## ⚠️ ԱՆՎՏԱՆԳՈՒԹՅԱՆ ԿԱՆՈՆՆԵՐ

### ❌ ԵՐԵՔԵԼԵՎ

```bash
# ❌ Force push main/develop-ում
git push --force origin main

# ❌ Գաղտնիքների commit
git add .env          # Պարունակում է գաղտնիքներ։
git add config.json   # Կարող է պարունակել բանալիներ։

# ❌ Shared ճյուղերի պատմության փոփոխում
git rebase main       # Եթե main արդեն սերվերում է
```

### ✅ ԱՆՎՏԱՆԳ ՊՐԱԿՏԻԿԱՆԵՐ

```bash
# ✅ Force push միայն իր ճյուղում
git push --force-with-lease origin feature/my-branch

# ✅ Ստուգում commit-ից առաջ
git diff --staged     # Ինչ ենք commit անում?
git status            # Արտասահման չկա՞

# ✅ Ինտերակտիվ rebase միայն լոկալ
git rebase -i HEAD~3  # Push-ից առաջ։
```

---

## 🔧 ՕԳՏԱԿԱՐ ԱԼԻԱՍՆԵՐ

```bash
# ~/.gitconfig

[alias]
  # Ստատուս և լոգեր
  st = status -sb
  lg = log --oneline --graph --decorate -20
  ll = log --pretty=format:'%C(yellow)%h%Creset %s %C(cyan)<%an>%Creset %C(green)(%cr)%Creset'
  
  # Ճյուղեր
  co = checkout
  cob = checkout -b
  br = branch -v
  
  # Commit-ներ
  cm = commit -m
  ca = commit --amend --no-edit
  
  # Sync
  sync = !git fetch origin && git rebase origin/main
  
  # Cleanup
  cleanup = !git branch --merged | grep -v 'main\\|develop' | xargs git branch -d
  
  # Undo
  undo = reset HEAD~1 --soft
  unstage = reset HEAD --
```

---

## 🏷️ ՏԱՐԲԵՐԱԿԱՎՈՐՈՒՄ

### Semantic Versioning

```
MAJOR.MINOR.PATCH
  │     │     │
  │     │     └── Patch. bugfix-եր, հետընթաց համատեղելի
  │     └──────── Minor. նոր ֆիչեր, հետընթաց համատեղելի
  └────────────── Major. breaking changes
```

### Git Tags

```bash
# Ստեղծել tag
git tag -a v1.2.0 -m "Release version 1.2.0"

# Push tag
git push origin v1.2.0

# Բոլոր tag-երը
git push origin --tags
```

### CHANGELOG

```markdown
# Changelog

## [1.2.0] - 2024-01-15

### Added
- Ապրանքների զամբյուղ
- Checkout գործընթաց

### Fixed
- Ուղղվել է աութորիզացիան

### Changed
- Թարմացվել է կոճակների դիզայնը

## [1.1.0] - 2024-01-01
...
```

---

## 📋 CHECKLIST

### Commit-ից առաջ

- [ ] `git diff --staged` — ստուգել եմ փոփոխությունները
- [ ] `.env`, գաղտնիքներ, API բանալիներ չկան
- [ ] `console.log`, debug կոդ չկա
- [ ] Հաղորդագրությունը համապատասխանում է Conventional Commits-ին
- [ ] Մեկ commit = մեկ լոգիկական փոփոխություն

### PR-ից առաջ

- [ ] Ճյուղը թարմացվել է main/develop-ից
- [ ] Թեստերն անցնում են լոկալ
- [ ] Lint/typecheck-ը անցնում է
- [ ] Self-review կատարվել է
- [ ] PR-ի նկարագրությունը լրացված է
- [ ] Կապված առաջադրանքներ (issues) կան

### Code Review

- [ ] Լոգիկան ճիշտ է
- [ ] Կոդը կարդացվող է
- [ ] Կրկնօրինակում չկա
- [ ] Թեստերը համապատասխան են
- [ ] Security խնդիրներ չկան
- [ ] Performance խնդիրներ չկան

---

**Տարբերակ.** 1.0
**Ամսաթիվ.** 2026-02-12
